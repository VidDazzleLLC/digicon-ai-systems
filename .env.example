# ============================================================================
# DIGICON AI SYSTEMS - Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values
# For Railway deployment, set these in Railway dashboard

# ============================================================================
# REQUIRED - Database
# ============================================================================
DATABASE_URL=postgresql://user:password@localhost:5432/digicon
# Railway will auto-populate this when you add PostgreSQL service

# ============================================================================
# REQUIRED - Application URLs
# ============================================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# Production (Railway): https://your-app.railway.app

# ============================================================================
# REQUIRED - Stripe Payment Processing
# ============================================================================
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
# Get webhook secret from Stripe Dashboard after setting up endpoint:
# https://your-app.railway.app/api/stripe/webhook

# ============================================================================
# REQUIRED - Email Delivery (Choose ONE option)
# ============================================================================

# Option 1: SMTP (Gmail, SendGrid, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
# For Gmail: Generate App Password at https://myaccount.google.com/apppasswords

# Option 2: Resend API (Recommended - easier setup)
RESEND_API_KEY=re_your_resend_api_key
# Sign up at https://resend.com (100 emails/day free)

# Email sender address (works with both SMTP and Resend)
EMAIL_FROM=noreply@digicon.app

# WARNING: If NEITHER SMTP nor Resend is configured, emails will NOT be sent!

# ============================================================================
# REQUIRED - AI Processing (Choose at least ONE)
# ============================================================================

# Anthropic Claude (Recommended - used for payroll analysis)
ANTHROPIC_API_KEY=sk-ant-your_anthropic_api_key

# OpenAI GPT (Alternative)
OPENAI_API_KEY=sk-your_openai_api_key

# Google Gemini (Alternative)
GEMINI_API_KEY=your_gemini_api_key

# ============================================================================
# OPTIONAL - Authentication
# ============================================================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_min_32_chars
# Generate with: openssl rand -base64 32

# ============================================================================
# OPTIONAL - Advanced Integrations
# ============================================================================

# Twilio (SMS/Voice notifications)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Supabase (Alternative to PostgreSQL)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Together AI (Advanced monetization features)
TOGETHER_API_KEY=your_together_api_key

# Aitable CRM Integration
AITABLE_API_KEY=your_aitable_api_key
AITABLE_SPACE_ID=your_aitable_space_id

# MissionX Integration
MISSIONX_API_KEY=your_missionx_api_key
MISSIONX_WORKSPACE_ID=your_missionx_workspace_id

# ============================================================================
# DEPLOYMENT SETTINGS
# ============================================================================
NODE_ENV=development
# Production: NODE_ENV=production

# ============================================================================
# QUICK START CHECKLIST
# ============================================================================
# Required for basic functionality:
# [ ] DATABASE_URL - PostgreSQL connection
# [ ] STRIPE_SECRET_KEY - Stripe API key
# [ ] STRIPE_WEBHOOK_SECRET - Stripe webhook signing secret
# [ ] ANTHROPIC_API_KEY - Claude AI for analysis
# [ ] SMTP_* OR RESEND_API_KEY - Email delivery
# [ ] NEXT_PUBLIC_APP_URL - Your app URL
#
# See RAILWAY_DEPLOYMENT.md for detailed setup instructions

